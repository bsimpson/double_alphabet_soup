# Javascript double alphabet soup word puzzle solver

Set `grid` to an array representing the scrambled letters of the given row

Run `node app.js`

# Ruby double alphabet soup word puzzle solver

Set `grid` to an array representing the scrambled letters of the given row

Run `ruby app.rb`
